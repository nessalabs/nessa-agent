{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "nessa://protocol/v1/server.json",
  "$defs": {
    "HealthResult": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "ok",
        "runtimeStatus",
        "uptimeMs"
      ],
      "properties": {
        "ok": {
          "type": "boolean",
          "description": "Whether the health probe succeeded."
        },
        "runtimeStatus": {
          "$ref": "common.json#/$defs/RuntimeStatus",
          "description": "Current runtime availability reported by the gateway."
        },
        "uptimeMs": {
          "type": "integer",
          "minimum": 0,
          "description": "Time since server startup in milliseconds."
        }
      },
      "description": "Gateway health response from client.server.health(). On product connections, access is authorized by the server."
    }
  }
}
